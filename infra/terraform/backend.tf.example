terraform {
  backend "s3" {
    bucket         = "pulsecity-tfstate"
    key            = "dev/terraform.tfstate" # change to "prod/terraform.tfstate" for prod
    region         = "us-east-1"
    encrypt        = true
    dynamodb_table = "pulsecity-tfstate-lock"
  }
}
