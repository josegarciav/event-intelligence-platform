apiVersion: v1
kind: Service
metadata:
  name: admin
  namespace: pulsecity
  labels:
    app: admin
spec:
  type: ClusterIP
  selector:
    app: admin
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
